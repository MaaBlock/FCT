
vec2 positions[3] = vec2[](
    vec2(0.0, -0.5),
    vec2(0.5, 0.5),
    vec2(-0.5, 0.5)
);

vec3 colors[3] = vec3[](
    vec3(1.0, 0.0, 0.0),
    vec3(0.0, 1.0, 0.0),
    vec3(0.0, 0.0, 1.0)
);

ShaderOut main(ShaderIn in) {
    ShaderOut out;
    out.color = colors[vertexIndex];
    out.pos = positions[vertexIndex];
    return out;
}